// SPDX-License-Identifier: MIT
// -----------------------------------------------------------------------------
// spyro-sdk-js ▸ utils ▸ errors.ts
// -----------------------------------------------------------------------------
// Central location for Spyro‑SDK custom error classes.  Using explicit
// subclasses (instead of raw `Error`) lets downstream apps discriminate
// between transport, contract‑level, or SDK mis‑use failures with
// `instanceof`.  Each error includes a machine‑rea...
