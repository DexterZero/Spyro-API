{
  "name": "spyro-adapter-render",
  "version": "0.1.0",
  "scripts": {
    "test": "npx asp --config as-pect.config.js",
    "build": "npx graph build"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.33.0",
    "@graphprotocol/graph-ts": "^0.30.1",
    "as-pect": "^7.0.1"
  }
}
