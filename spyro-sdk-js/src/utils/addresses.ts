// SPDX-License-Identifier: MIT
// -----------------------------------------------------------------------------
// spyro-sdk-js ▸ utils ▸ addresses.ts
// -----------------------------------------------------------------------------
// Centralised helper for loading contract addresses.  By default it reads the
// JSON artifact produced by `spyro-contracts/script/deployAll.ts` (spyro‑all.json)
// but can also fall back to ENV vars so browser bundles or dApps deployed to
// Vercel / Netlify can inject addresse...
